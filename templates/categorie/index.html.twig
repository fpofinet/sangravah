{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
<section class="row">
    <div class="col-lg-8 col mt-5 mx-auto">
        <div class="container mt-5">
            <div class="container-fluid p-1">
                <a href={{path("new_categorie")}} class="btn btn-success btn-lg">Ajouter</a>
            </div>
            <div class="container border p-0 rounded  shadow mt-3 mb-5">
                <p class="h2 fw-bold text-center text-light mb-2 bg-dark rounded-top">Liste des categories</p>
                <div class="p-3">
                    <table class="table table-striped table-responsive" id="myTable">
                        <thead class="">
                        <tr>
                            <th>N°</th>
                            <th>CODE</th>
                            <th>LIBELLE</th>
                            <th>TYPE</th>
                            <th>SEXE</th>
                            <th></th>
                            <th></th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for cat in categories %}
                            <tr>
                                <td>1</td>
                                <td>{{cat.code}}</td>
                                <td>{{cat.libelle}}</td>
                                <td>{{cat.type}}</td>
                                <td>{{cat.sexe}}</td>
                                <td><a href={{path("update_categorie",{'id':cat.id})}} class="btn btn-outline-success">Modifier</a></td>
                                <td><a href={{path("delete_categorie",{'id':cat.id})}} class="btn btn-outline-danger">Supprimer</a></td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>
<script>
    let table = new DataTable('#myTable', {
    // config options...
});
</script>
{% endblock %}
